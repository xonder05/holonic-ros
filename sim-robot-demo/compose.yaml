services:
  sim:
    image: sim-robot-demo
    environment: # -e
      - DISPLAY=${DISPLAY}
    volumes: # -v
      - /tmp/.X11-unix:/tmp/.X11-unix
    stdin_open: true # -i
    tty: true # -t
    command: bash
